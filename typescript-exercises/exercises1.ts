// Create a task interface with these properties:
//    id, title, description, dueDate (all strings)

// Create a priority Enum that has the following constants:
//   Low, Medium, High

// Create a TaskPriority interface that extends Task 